// Generated by CoffeeScript 1.6.3
/*
 Detail State when a ParticleView has been clicked
*/


(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['AppController'], function(AppController) {
    var DetailView;
    return DetailView = (function(_super) {
      __extends(DetailView, _super);

      function DetailView(width, height) {
        this.width = width;
        this.height = height;
        this.onHideComplete = __bind(this.onHideComplete, this);
        DetailView.__super__.constructor.call(this);
        this.color = '#0AA5DD';
        this.bg = new createjs.Shape();
        this.addChild(this.bg);
        this.bg.alpha = 0.9;
        this.mask = new createjs.Shape();
        this.mask.scaleX = 0;
        this.mask.scaleY = 0;
        this.LINE_WIDTH = 800;
        this.txtContainer = new createjs.Container();
        this.addChild(this.txtContainer);
        this.headerTxt = new createjs.Text("PROPHET @ProphetBrand", "50px kiosk_black", "#405462");
        this.headerTxt.lineWidth = this.LINE_WIDTH;
        this.txtContainer.addChild(this.headerTxt);
        this.bodyTxt = new createjs.Text("Lorem ipsum dolor sit amet, consectetur adipiscing elit. Vestibulum turpis nisi, tincidunt eget iaculis id, malesuada et eros. Vestibulum luctus faucibus lacus vitae dignissim.", "30px kiosk_light", "#ffffff");
        this.bodyTxt.y = 75;
        this.bodyTxt.lineWidth = this.LINE_WIDTH;
        this.txtContainer.addChild(this.bodyTxt);
        this.visible = false;
        this.addEventListener("click", this.handleMouseEvent);
      }

      DetailView.prototype.update = function(data) {};

      DetailView.prototype.show = function() {
        this.visible = true;
        return TweenLite.to(this.mask, 0.3, {
          scaleX: 1.0,
          scaleY: 1.0
        });
      };

      DetailView.prototype.hide = function() {
        return TweenLite.to(this.mask, 0.3, {
          scaleX: 0.0,
          scaleY: 0.0,
          onComplete: this.onHideComplete
        });
      };

      DetailView.prototype.onHideComplete = function() {
        return this.visible = false;
      };

      DetailView.prototype.handleMouseEvent = function(e) {
        return AppController.hideDetail();
      };

      DetailView.prototype.resize = function(w, h) {
        this.width = w;
        this.height = h;
        this.bg.graphics.clear();
        this.bg.graphics.beginFill(this.color).drawRect(0, 0, this.width, this.height);
        this.mask.x = this.width / 2;
        this.mask.y = this.height / 2;
        this.mask.graphics.clear();
        this.mask.graphics.beginFill('#ffffff').drawCircle(0, 0, this.width);
        this.txtContainer.x = this.width / 2 - this.LINE_WIDTH / 2;
        return this.txtContainer.y = this.height / 2 - 200;
      };

      DetailView.prototype.destroy = function() {
        this.uncache();
        return this.removeAllEventListeners();
      };

      return DetailView;

    })(createjs.Container);
  });

}).call(this);
